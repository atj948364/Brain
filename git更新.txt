# 检查当前分支
git branch

# 切换到正确的分支（如果需要）
git checkout <branch-name>

# 检查状态
git status

# 添加更改到暂存区
git add .
git add files

# 提交更改
git commit -m "Your commit message"

# 推送更改到远程仓库
git push origin <branch-name>